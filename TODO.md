# 작업 예정 목록

지금 당장 진행하지 않고 미뤄둔 기능 요청을 적어둔다. "나중에 작업해"라고
명시적으로 요구받으면 그때 해당 항목을 꺼내 구현한다.

## 신규 개발 트랙: 감마 버전 (COM 비의존, rhwp 기반) — 사용자 지정, 2026-09-25

**방향**: 지금까지의 버전(알파/베타, 모두 `win32com.client`로 Windows +
한/글 COM에 의존)과 별도로, **COM에 전혀 의존하지 않고 오직 `rhwp`
기반으로 후처리 기술을 구현**하는 감마 버전을 개발한다.

**용어 정의(사용자 확인, 2026-09-25)**:
- **정규 버전** — 지금 배포 중인 `hwp-auto-docfit`(COM 기반) 그 자체.
- **베타** — 정규 버전의 기능 개선판(같은 COM 기반 계열, 정규 버전 다음
  릴리스로 보임).
- **알파** — 신기술 테스트용 버전(정규/베타보다 실험적인 새 기술을
  먼저 시험해보는 트랙).
- **감마** — 바로 이 rhwp 기반 버전(COM 비의존).

**rhwp 기술 검토** (착수 전 조사):
- `rhwp`(GitHub `edwardkim/rhwp` 등, npm `@rhwp/core`)는 Rust+WASM 기반
  HWP/HWPX 파서·렌더러(SVG 출력)이자, "HWP 편집 저장, HWPX/HML 의미
  보존 저장"까지 지원하는 비교적 성숙한 프로젝트다 — 단순 읽기 전용이
  아니라 편집·저장 경로가 있음. `hwpx-skill`(별도 조사한 Claude
  스킬)도 HWP→HWPX 변환의 "WASM 폴백" 경로로 이미 rhwp를 쓰고 있다.
- **결정적 제약 하나**: rhwp는 텍스트 폭 측정을 직접 하지 않고, 호출자가
  `measureTextWidth(font, text)` 콜백을 주입해야 한다(브라우저/Node의
  Canvas 2D 컨텍스트로 재는 게 일반적 쓰임으로 보임). 즉 "이 텍스트가
  실제로 어디서 줄바뀜하는지"를 rhwp 스스로 알려주지 않는다.
- 우리 앱의 핵심 알고리즘(자간/장평으로 줄 끝 단어분리 방지)은 지금
  COM으로 한/글 자신에게 실제 커서 위치를 물어봐서 얻으므로 항상
  정확하다(그래서 [1순위] 부연설명 안전화 항목에서도 "계산 대신 실측"이
  해법이었다). rhwp 경로로 가면 이 실측을 **COM 없이 우리가 직접
  재현**해야 하는데, 이러려면 최소 두 가지가 필요하다:
  1. 실제 한/글 번들 폰트(함초롬바탕·신명조·휴먼명조·HY견고딕 등)의
     정확한 글리프 폭 데이터 — 이 폰트들은 한컴 소유로 무료 배포되지
     않아 구하기 자체가 문제일 수 있음.
  2. 한/글 고유의 줄채움·양쪽정렬 자간 늘림 알고리즘 재현 — 이건 사실상
     한/글 레이아웃 엔진을 역공학하는 수준의 작업.
- Python 바인딩 없음, JS/Node/WASM 전용 — 지금 앱은 순수 Python이라
  감마 버전은 사실상 별도 스택(Node.js 기반 재작성, 또는 Python↔Node
  IPC 브리지)이 필요하다.

**리스크 프로필이 근본적으로 다름**: 알파(COM)는 "Windows+한/글
필수"라는 배포 제약이 있지만 결과는 항상 정확하다(실제 렌더링을 그대로
읽으므로). 감마는 그 배포 제약을 없애는 대신, "한/글의 실제 렌더링
결과를 몰라도 정확히 재현해야 한다"는 훨씬 어려운 문제로 바꾸는
셈이다 — 부담이 없어지는 게 아니라 이동한다.

**제안하는 시작 범위(전면 재구현 대신 좁게)**: 처음부터 자간/장평
word-wrap 방지 전체를 rhwp로 재현하려 하지 말고, **렌더링에 의존하지
않는 "구조적" 후처리**부터 감마로 옮기는 게 현실적이다 — 예:
- C1/C2/C3(표 셀 여백/너비/테두리) — XML 속성값 수정만으로 끝남,
  레이아웃 측정 불필요.
- 문두기호 굵게/색 처리, 괄호 라벨 서식 — 텍스트 패턴 매칭 + 서식
  속성 설정, 레이아웃 불필요.
- 자간/장평 기반 줄바꿈 방지는 위 두 가지 제약(폰트 데이터·줄채움
  알고리즘 재현)이 해소되기 전까지는 감마 범위에서 제외하고 알파(COM)
  전용으로 남겨두길 권장.

**착수 전 확인 필요**:
- 감마 버전의 목적 — Windows+한/글이 없는 환경(Mac/Linux) 사용자
  지원이 목표인지, 이 개발 세션의 실환경 검증 지연(Linux 샌드박스에서
  COM을 못 쓰는 문제) 해소가 목표인지에 따라 우선순위 범위가 달라진다.
- Node.js 런타임을 어떻게 포함할지(사용자 PC에 설치를 요구할지, 번들할지).
- rhwp의 편집·저장 API가 문단 서식 속성(자간/장평/들여쓰기 등)까지
  세부적으로 노출하는지 — 이번 조사로는 확인 못 했음(공개 API 문서가
  제한적). 실제 사용 전 rhwp 저장소의 API 레퍼런스를 더 깊이 확인
  필요.

## (폐기) Kordoc 기술 의존성 제거

2026-09-25에 장기 계획으로 기록했으나, 같은 날 사용자가 목표 자체를
폐기(취소)함. `kordoc_bridge.py` 기반 "고급 도구" 6개 기능(표
추출·분류·양식 필드 분석·서식 보존 패치·공문서 표기법 검수·페이지
이미지 미리보기·Markdown→HWPX 생성)은 지금처럼 kordoc에 계속 의존해도
됨 — 교체 계획 없음.

## 확정된 개발 방향: A (서식 자동화 심화)

실무 보고서(5~10쪽, 총 10시간 기준) 작성 8단계의 시간 비중 데이터를
검토한 뒤 사용자가 확정: **A안(서식 자동화 심화 — 리스크 낮음, 상한
명확)**으로 간다. B안(자료 수집·본문 작성까지 AI가 관여하는 콘텐츠
지원으로 확장, 상한 훨씬 높지만 리스크·정체성 변화 큼)은 채택하지
않음.

이에 따라 "다음에 진행할 작업"의 우선순위를, A안이 다루는 3위(서식
적용·시각화 13.3%)·4위(최종 검토·교정 8.3%)·5위(쪽 맞추기 6.7%)·
6위(자간·행간 미세조정 5.8%) 단계에 대한 기여도를 기준으로 재정렬한다.
숫자는 "실무 보고서 8단계 시간 비중 검토" 보고에서 인용.

1. ~~**부연설명(`*`/`※`) 들여쓰기 안전화**~~ **(완료 2026-09-25)** — 시간 비중 순위와 무관하게
   최우선 배치(사용자 지정, 2026-09-25). 정확성 리스크(양쪽정렬에서
   위치가 미묘하게 어긋날 수 있는 잠재 버그) 해소가 새 기능보다 먼저.
   설계는 아래 "잠재 버그 의심" 절 참고, 착수 전 확인사항 있음.
2. ~~**C1/C2/C3(표 구조 정밀 조정) 실 한/글 검증**~~ **(완료 2026-09-25)** — 3위 단계의 "표 작성"
   부분에 직접 기여, 이미 구현은 끝났고 검증만 남음(가장 빠르게 가치를
   확인할 수 있음).
3. ~~**서식통일 기능 신설**(+ 작업모드 재구성)~~ **(완료 2026-09-25)** — 3위(서식 일관성)와
   4위(교정·교열 중 스타일 오류 확인)에 걸침. 지금 앱에 없는 새 능력.
4. **최종검수 확장 — 숫자 데이터 일치 검사** — 4위 단계가 명시한 핵심
   작업("본문 텍스트의 숫자와 표/그래프 수치 일치 여부 확인")을 정면으로
   겨냥. 다만 아직 설계 착수 전(신규 항목, 아래 참고).
5. **아웃라이너 기능 강화** — 7위(목차 구성, 4.2%)에 기여, 비중이 작아
   후순위.
6. **서식 예시 "직접 편집" 추가** — 3위 단계를 위한 준비 작업(서식 등록)을
   편하게 하는 UX 개선. 반복 작업 자체를 줄이진 않아 후순위.
7. **윈도우 스티커 메모 연동** — 8단계 시간 비중 어디에도 명확히
   대응되지 않음. A 방향의 우선순위 기준으로는 근거가 약하니, 착수 전에
   "왜 필요한지"부터 다시 확인.

(자료 수집·본문 초안 작성을 돕는 "보고서 작성 스킬" 논의는 B안 성격이라
A안 확정 이후 보류였으나, 사용자가 완성된 Claude Skill 패키지
(`gongmunseo-report-writing`, 「칼퇴를 위하여」 1~5권 기반)를 직접 만들어
전달함에 따라 `.claude/skills/gongmunseo-report-writing/`에 추가 완료.
이 앱(hwp-auto-docfit) 코드와는 무관한 사용자 개인 작성 보조 스킬이며,
A안 우선순위 목록과는 별개로 취급한다.

스킬 추가 후 "이 스킬을 포함해 우선순위를 다시 검토해야 하는가"를
재검토했고, 결론은 **재정렬 불필요**. 두 도구는 실행 표면이 달라 시간
비중을 단순 합산할 수 없다 — 앱은 HWP 파일에 무인 일괄 자동화를
적용해 해당 단계 시간을 거의 0으로 만들지만(그래서 A안의 "리스크
낮음·상한 명확"이 성립), 스킬은 Claude와의 대화 속에서 문체·구조를
조언할 뿐 최종 집필은 사람이 한다. 스킬이 커버하는 2위(본문작성)·
4위(교정) 비중을 앱의 25.8%에 더해 "이제 대부분 해결됐다"고 보면 안
됨. 특히 1위 단계(자료수집·검증, 33.3%, 8단계 중 최대)는 스킬도 전혀
다루지 않는 영역으로 여전히 완전한 공백이며, 이는 사실 검색·출처
신뢰성 검증이라는 스킬의 범위(문체·논리 정리) 밖의 별개 문제라 지금
단계에서는 손대지 않는다.

교차 발견 하나: 앱의 **[4순위] 숫자 데이터 일치 검사**(본문 텍스트
숫자 ↔ 표 수치 기계적 대조, 4위 단계 겨냥)와 스킬의 6단계 "퇴고"가
개념적으로 같은 문제를 겨냥한다. 스킬 쪽 `SKILL.md` 퇴고 체크리스트에
숫자 대조 항목을 추가 완료(저비용 문서 수정이라 앱 우선순위와 무관하게
바로 반영). 앱 쪽 [4순위] 항목은 기계적·결정론적 대조라는 점에서 여전히
독립된 가치가 있어 그대로 유지한다.

**유사 스킬·앱 조사 결과(웹 검색, 2026-09-25):** 동일한 결 — 「칼퇴를
위하여」 기반 어휘 뉘앙스 사전 + A+B 문장구조 철학을 갖춘 도구는 없음.
다만 "개조식 문서 작성 보조"라는 목적 자체는 이미 여러 도구가 다룸.
- **가장 근접**: `NAMYUNWOO/korean-itemization` (GitHub, MIT) — 개조식
  6원칙(한 항목 한 사실, 두괄식+키워드, 명사형 종결, 중복 제거, 병렬성,
  위계)을 가이드하는 Claude 스킬. 다만 형식·문장종결 규칙 중심이고,
  어휘 뉘앙스 사전이나 "왜 이 단어를 고르는가" 층위는 없음. 저자명이
  「칼퇴를 위하여」 저자 남윤철과 발음이 비슷하나 동일인 여부 불명,
  내용상 그 책을 인용하지 않아 무관해 보임.
- `JangHyun-bin/korean-report-skills` — 134개 치환 규칙 기반 린팅(번역투·
  모호한 수치·근거 없는 주장 검출) + HTML/PDF 출력 디자인. 검수·시각적
  출력에 방점, 방법론(규칙 기반 린터)이 우리 스킬과 다름.
- `korean-business-document-generator`(mcpmarket) — 민간기업 주간보고·
  기안·품의·회의록 대상, 결재라인 구조화. 정부기관 개조식이 아니라
  회사 격식체 초점.
- 범용 AI 보고서 도구(한컴어시스턴트, ChatGPT GPTs 등)는 뉘앙스 사전이나
  A+B 구조 근거 제시 없이 "그냥 써준다"는 접근이라 결이 다름.
- (참고, 스킬이 아니라 앱 쪽 비교군) **범정부오피스** — 행안부가 2023년
  배포한 무료 공문서 서식 자동화 프로그램, 20여 개 기관 도입, 1,000여
  기능(폰트·글머리·간격 일괄 정리, 어절 잘림 방지 자동 자간, AI 초안
  생성 등). 이건 `gongmunseo-report-writing`(문체 스킬)보다
  `hwp-auto-docfit`(서식 자동화 앱) 쪽과 기능이 크게 겹쳐, 앱 로드맵
  검토 시 참고할 실사용 경쟁 사례로 별도 기록해둔다.

결론: 지금 스킬을 교체하거나 저 스킬들을 가져다 쓸 필요는 없음(뉘앙스
사전 + 원저자 철학이라는 차별점이 뚜렷함). 다만 `korean-itemization`의
형식 규칙(명사형 종결, 위계 nesting 제한 등)은 우리 `formatting.md`가
아직 다루지 않는 부분이 있는지 향후 한 번 대조해볼 가치는 있음.)

## [1순위, 최우선] 부연설명(`*`/`※`) 들여쓰기 안전화 — 양쪽정렬에서 어긋날 가능성

> **완료(2026-09-25, 1.68 Alpha 6, `alpha` 브랜치)** — 아래 "순서 재배치안"대로 구현하고
> 실제 한/글에서 검증했다.
> - 실측 결과 잠재 버그가 **실제로 있었다**: 예전 계산 폭은 한/글 실측값의 절반 수준(예:
>   부모 `  - ` 계산 2800 / Shift+Tab 실측 5600, `- (운영방식) “…` 계산 10276 / 실측
>   19944). 또 부연설명의 선행 공백을 첫 줄 값(-w_lead)으로 "상쇄"하던 방식은 한/글에서
>   음수 첫 줄 값이 '나머지 줄을 들여쓰는' 내어쓰기라 마커가 선행 공백만큼 밀렸다.
> - 구현: `_캐럿위치_폭_실측()`(캐럿을 둔 채 실제 Shift+Tab 실행 → 값 읽기 → 원래 첫 줄 값
>   복원)으로 부모 본문 시작(왼쪽여백+실측 폭)과 부연설명 선행 공백 폭을 모두 실측하고,
>   부연설명 왼쪽여백 = 부모 본문 시작 − 선행 공백 폭으로 둔다(자기 내어쓰기는 유지).
> - 순서: '부연설명 들여쓰기'를 '최종 서식 기준 내어쓰기' 뒤로 옮겼고, 자간 조정 반복에서
>   부모 내어쓰기가 바뀌면 딸린 부연설명도 다시 맞춘 뒤 단어 분리 재검사 대상에 넣는다.
> - 검증: 통합테스트 문서 사본에 적용해 쪽 이미지(`CreatePageImage`)로 마커가 부모 본문 첫
>   글자와 같은 칸에 오는 것을 눈으로 확인.
> - 참고: 이 기능의 설정(`std_supplement_indent`, 설정의 "설명 위치")은 기본값이 꺼져 있다.

**계기**: 범정부오피스의 기술 평가("왼쪽정렬은 내어쓰기 안 틀어지는데
양쪽정렬은 미묘하게 틀어져서 하드코딩으로 때웠다")를 보고 우리 앱에
같은 문제가 없는지 점검한 결과.

**결론**: 본문 내어쓰기(`문단_내어쓰기_적용`, hwp-auto-docfit.py:2900)는
안전함 — 폭을 계산하지 않고 실제 커서를 옮겨 한/글의 진짜 Shift+Tab
명령(`ParagraphShapeIndentAtCaret`)을 실행하므로, 정렬이 무엇이든 한/글
엔진 자신이 그 시점 렌더링을 보고 값을 정한다(2807행 주석: "정렬을
변경하지 않고 실제 Shift+Tab으로 처리한다").

**그러나 부연설명(`*`/`※`) 들여쓰기 기능은 다른 방식이라 위험이
남아있을 수 있음.** `문단_선행부_폭_HWPUNIT()`(2822행)가 문자 폭을
"글자 크기 × 장평 × 전각/반각 배수"로 **계산해서 합산**하는데, 공백은
항상 고정 0.5em으로 가정한다. docstring은 "정렬과 무관하게 항상 같은
값이 나온다(결정적)"이라 주장하지만, 이건 글자 모양 자체만 고려한
것이지 **양쪽정렬이 만드는 여분의 자간/어간 늘림**은 반영하지 않는다.
부모 문단의 선행부(문두기호+공백)에 공백이 있으면, 양쪽정렬 문서에서
그 공백이 실제로는 늘어나 있을 수 있어 계산된 폭이 실제 렌더링 위치와
미묘하게 어긋날 가능성 — 범정부오피스가 겪은 것과 같은 클래스의 버그.

**확인 필요**: 이 계산은 렌더링 의존적이라 이 환경(Linux 샌드박스)에서
검증 불가. 실제 양쪽정렬 문서에 부연설명(`*`/`※`)이 있고 그 바로 위
부모 문단(제목/본문/내용)의 문두기호 뒤에 공백이 있는 케이스로 Windows
+ 한/글에서 실제 테스트 필요. 어긋남이 확인되면, 본문 내어쓰기처럼
"계산" 대신 "실제 커서 이동 + 한/글 명령"으로 바꾸는 방향을 검토.

### 개선 제안 (설계만, 착수 전) — 부연설명도 "계산" 대신 "실측"으로

본문 내어쓰기가 안전한 이유는 폭을 계산하지 않고 실제 Shift+Tab을
실행해 한/글이 정한 값을 그대로 쓰기 때문이다. 부연설명도 같은 원리로
바꿀 수 있다.

1. **부모_W 실측화**: `문단_선행부_폭_HWPUNIT()` 호출 대신, 부모
   문단(제목/본문/내용)에 이미 있는 `문단_내어쓰기_기준_오프셋(text)` +
   `문단_내어쓰기_적용(문단_시작, text, ...)`을 그대로 실행한다. 이 함수는
   "기존 내어쓰기를 지운 동일한 첫 줄 상태에서 매번 계산한다"는 멱등적
   설계라 여러 번 호출해도 안전하다. 성공하면
   `hwp.ParaShape.Item("Indentation")`의 절댓값이 한/글이 실제 렌더링을
   보고 계산한 진짜 폭이며, 이걸 그대로 부모_W로 쓴다.
2. **부연설명 자신의 선행공백(w_lead)도 같은 방식으로 실측**: 지금은
   `_선행공백_길이()` + `문단_선행부_폭_HWPUNIT()`로 계산하지만, 이것도
   "그 오프셋까지의 폭을 한/글에게 물어봐서 읽기"로 바꿀 수 있다. 다만
   `문단_내어쓰기_적용()`은 지금 "문두기호+라벨 뒤 본문 시작"이라는
   의미의 오프셋을 전제로 설계돼 있어, "단순 선행 공백 길이"라는 다른
   개념의 오프셋도 받아들이도록 내부 계산 부분만 분리하는 리팩터링이
   먼저 필요하다(오프셋 의미를 매개변수로 일반화).
3. **파이프라인 순서 문제**: 지금은 "부연설명 들여쓰기"(7730행)가 "최종
   서식 기준 내어쓰기"(7749행, `문단_내어쓰기_전체_갱신`)보다 **먼저**
   실행된다. 1번 방식대로 부모 문단에 미리 `문단_내어쓰기_적용()`을
   실행해두면, 뒤이어 실행되는 "최종 서식 기준 내어쓰기"가 같은 문단을
   다시 처리해도 멱등적이라 값이 같게 나오므로 순서를 안 바꿔도 되지만,
   **매번 새로 계산**하는 셈이라 COM 왕복이 늘어 처리 속도에 영향을 준다
   (이 세션에서 이미 처리속도를 한 번 점검한 적이 있어 트레이드오프로
   짚어둘 필요가 있음).
   - **대안**: 아예 파이프라인 순서를 바꿔 "부연설명 들여쓰기"를 "최종
     서식 기준 내어쓰기" **뒤**로 옮기면, 부모 문단은 이미 실측된
     Indentation 값을 갖고 있으므로 **다시 계산할 필요 없이 그 값을
     그대로 읽기만** 하면 된다 — 추가 COM 호출이 거의 없어 속도 저하가
     적다. 다만 `표준서식_내어쓰기_사용=False`로 본문 내어쓰기 자체가
     꺼져 있는 경우엔 부모 문단에 실측값이 없으므로, 그때만 기존
     폭 계산 방식(`문단_선행부_폭_HWPUNIT`)으로 폴백해야 한다.
   - 두 방식 중 **순서 재배치안이 더 낫다고 판단**한다 — 코드 변경 범위가
     작고, 추가 COM 왕복이 없어 처리 속도 저하가 없다.

**착수 전 확인 필요**: 순서를 바꿨을 때 다른 단계(자간 조정 등)가
"부연설명 들여쓰기가 먼저 끝나 있다"는 걸 전제로 하는 곳이 있는지
전체 파이프라인(7700~7800행 부근)을 재확인해야 한다.

## 앱 UI/UX 평가 및 개선 제안 (2026-09-25)

Nielsen 사용성 휴리스틱 기준, 코드로 실제 UI 텍스트·동작을 확인해 평가.

**강점(확인됨)**: 메인 화면 3카드가 쉬운 말+색상 구분(9496~9498행),
체크박스 아래 힌트 라벨(`STAGE_EXAMPLES`), "세부 설정…" 버튼이 카드
옆에 이미 노출(9516행), 세부 설정이 실제 처리 7단계와 1:1로 재구성돼
있음(38~40번 기능), 원본을 절대 덮어쓰지 않고 항상 "(자간조정)" 등
접미사 붙은 새 파일로 저장(`저장파일명()`, 7690행), 원본·결과 비교
보기·페이지 이미지 미리보기·자간조정 결과 검수 등 확인 장치 다수,
완료 팝업의 성공/실패 건수 표시, 실행/중단/이어서 진행 지원.

**약점 및 개선 제안 (우선순위)**:
1. **실행 버튼 근처에 "원본은 그대로, 새 파일로 저장됩니다" 문구
   상시 노출** — 이미 있는 안전장치(원본 미덮어쓰기)가 화면 텍스트로
   명시돼 있지 않아, 불안한 초보자가 실행을 주저할 수 있음. 가장
   저비용·고효과 개선.
2. **최초 실행 시 3줄 온보딩 모달(또는 "?" 버튼)** — 앱 내
   도움말/가이드/사용법 관련 코드가 0건. "① 문서를 넣는다 ② 카드를
   고른다 ③ 실행을 누른다" 수준의 최소 안내조차 없어, README(외부
   파일)를 안 보고 온 초보자는 전체 그림을 알기 어려움.
3. **"세부 작업"(실행창)과 "세부 설정"(설정창) 명칭 재검토** — 같은
   값을 공유하는 서로 다른 두 창인데 이름이 비슷해 혼동 소지. "세부
   작업"을 "이번 실행에 적용"처럼 동작 중심 이름으로 바꾸는 방안 검토.

착수 전 확인 필요: 온보딩 모달의 구체적 트리거 방식(최초 실행만? 매번
"?" 버튼?)과 문구 확정은 사용자 확인 후 진행.

### 설정 변경 시 실시간 미리보기 (사용자 선택, 설계만·착수 전)

**요청**: "정적 인터페이스"를 "동적 인터페이스"로 — 설정을 바꾸면 결과가
바로 보이는 실시간 미리보기.

**실현 가능성**: 이미 있는 부품으로 조립 가능. `render_preview`(HWP를
이미지로 렌더링, 12460행 "페이지 이미지 미리보기"), "GUI/HWP 작업
스레드 분리"(21번 기능), 단독 HWP 세션 패턴(`텍스트_hwpx_단독변환` 등)
— 이 셋을 조합하면 새로 만들 부분보다 재사용할 부분이 많다.

**설계**:
1. **미리보기 패널** — 세부 설정 창에 우측 열(또는 하단)로 미리보기
   영역 추가, `render_preview` 재사용.
2. **미리보기 소스 선택(사용자 토글)** — (a) 내장 샘플 문단(빠르고
   예측 가능) vs (b) 지금 선택한 실제 문서의 첫 페이지(실감나지만
   무거움). 기본값은 (a) 권장.
3. **트리거 — 디바운스 필수(예: 400ms)** — 체크박스/스핀박스 변경
   이벤트에 연결하되, COM 왕복이 느리다는 걸 이 세션에서 이미 확인했으므로
   스핀박스 연타 시마다 즉시 재처리하면 버벅임. 마지막 변경 후 일정
   시간 지나야 재처리하도록 지연 처리 필요.
4. **격리 필요** — 배치 작업 중에는 미리보기용 HWP 세션이 배치와
   리소스(HWP 인스턴스)를 다투지 않도록 별도 세션으로 분리해야 함.
5. **범위 제한(1차)** — 처음부터 모든 설정에 붙이지 말고, 눈에 바로
   보이는 서식류(폰트·자간 최대치·문두기호 서식·내어쓰기)부터 시작.
   표 구조 정밀조정(C1/C2/C3, [2순위], 아직 실 검증 전)은 안전성부터
   확인 후 미리보기에 포함하는 순서가 맞음.

**착수 전 확인 필요**: 미리보기 소스 기본값((a) 내장 샘플 vs (b) 실제
문서)과, 1차로 미리보기를 붙일 설정 항목의 구체적 범위를 사용자에게
확인 후 진행.

### 사용자 취향에 맞는 스킨(테마) 선택 기능 (사용자 요청, 설계만·착수 전)

**실현 가능성 매우 높음** — 이미 있는 구조에 딱 맞는 요청. 앱에 이미
`_테마_적용()`이라는 전용 함수와 `UI_COLORS`라는 단일 팔레트
dict(teal/orange/rose/brown/lavender/teal_dark/teal_tint 등, 9042~
9046행)가 있어, 모든 ttk 스타일과 캔버스 일러스트(마스코트 등,
9280~9307행)가 이 dict 하나를 참조해 색을 입힌다. "테마"라는 개념
자체가 이미 코드에 있어, 이번 요청은 완전히 새로운 아키텍처가 아니라
"팔레트를 여러 개 만들고 고르게 하기"에 가깝다.

**설계**:
1. `UI_COLORS`를 여러 팔레트로 확장(예: 기본(청록)/다크모드/파스텔/
   고대비 등) — 각 팔레트는 같은 키를 가진 dict로 통일.
2. 설정창에 "스킨 선택" 콤보박스 추가 → 선택 시 `UI_COLORS`를 교체하고
   `_테마_적용()`을 재호출해 **재시작 없이 즉시 반영** — 바로 위
   "실시간 미리보기" 항목과 같은 "설정 변경 → 즉시 반영" 패턴 재사용.
3. 선택값은 기존 설정 저장 메커니즘(%APPDATA%, 19번 기능)에 함께
   저장해 다음 실행 시 유지.
4. 캔버스 일러스트도 `c` 인자로 `UI_COLORS`를 받는 구조라 별도 작업
   없이 자동으로 스킨을 따라갈 가능성이 높음 — 실제로는 확인만 필요.

**착수 전 확인 필요**:
- 몇 종류의 스킨을 제공할지(예: 기본/다크모드/고대비 3종 정도로
  시작할지, 사용자가 색상을 직접 지정하는 커스텀까지 허용할지).
- 다크모드를 포함한다면 단순 팔레트 교체를 넘어, 로고·아이콘 이미지가
  밝은 배경 전제로 만들어졌을 경우 다크 배경에서 잘 안 보일 수 있어
  이미지 쪽도 별도 점검 필요.

### 서식 종류를 이미지 미리보기로 직관적 선택 (사용자 요청, 설계만·착수 전)

**현재 상태**: 서식 프로파일 선택은 순수 텍스트 콤보박스뿐(9531~9532행,
`이름들 = [p["name"] for p in ...]`, 10602행) — 실제로 어떤 서식인지
이름만 보고 골라야 함.

**실현 가능성**: `render_preview()`(`docfit_core/kordoc_bridge.py:112`)가
이미 임의의 .hwp/.hwpx 파일을 PNG로 렌더링하는 기능을 갖고 있다 —
COM이 아니라 외부 `kordoc` CLI("Optional bridge to the MIT-licensed
kordoc v4 document engine")를 통해 동작하는 **선택적 의존성**. 썸네일
생성·표시 패턴(PIL `Image.thumbnail()` + `ImageTk.PhotoImage`)도
캐릭터 이미지 처리에서 이미 검증돼 있다(10498~10542행).

**막히는 지점**: 프로파일 JSON은 원본 예시 파일 경로나 이미지를 저장
하지 않는다(`_서식_복사완료`, 순수 스타일 값 dict만 저장) — 지금
당장 "저장된 프로파일을 렌더링할 실제 문서"가 없다.

**설계안**:
1. 프로파일 저장 시점(`_서식_복사완료`)에 썸네일 PNG를 만들어 프로파일
   폴더에 함께 저장(`{identifier}.png`) — 두 방식 중 선택 필요:
   - (a) 사용자가 드래그한 원본 예시 문서를 그대로 `render_preview()`로
     렌더링해 1페이지를 썸네일로 저장(간단하지만, 원본에 민감정보가
     있으면 그대로 썸네일에 노출됨).
   - (b) 프로파일 스타일을 적용한 짧은 합성 샘플 문서(제목·ㅇ·- 등
     계층 예시 2~3줄)를 만들어 렌더링(더 안전하지만 합성 문서 생성
     로직 추가 필요, "서식 예시 직접 편집"[6순위] 설계와 일부 겹침).
2. 설정창의 프로파일 콤보박스 옆(또는 대체)에 **그리드형 갤러리** 추가
   — 각 칸에 썸네일+이름, 클릭하면 선택.
3. `kordoc` 미설치 환경에서는 썸네일 생성이 실패할 수 있으므로, 실패
   시 기존 텍스트 콤보박스로 자동 폴백 — 필수 기능이 아니라 있으면
   좋은 기능으로 설계.

**착수 전 확인 필요**:
- (a)/(b) 중 어떤 방식으로 썸네일을 만들지 — 민감정보 노출 우려와
  직결되므로 사용자 확인 필요.
- 갤러리를 콤보박스 옆에 추가만 할지, 완전히 대체할지.
- `kordoc`이 실제로 사용자 PC에 설치돼 있는지부터 먼저 확인 필요 —
  선택적 의존성이라 없으면 이 기능 자체가 안 보일 수 있음.

## 스킬 경쟁력 평가 (`gongmunseo-report-writing`, 2026-09-25)

**핵심 경쟁력**: "근거 있는 뉘앙스 판단 + 즉시 완성형 산출물 + 정부
서식 정합성"의 조합. 비교군(`korean-itemization`,
`korean-report-skills`, `korean-business-document-generator`, 범용 AI
보고서 도구) 중 이 세 가지를 동시에 갖춘 도구는 없음.

**강점**:
- 어휘 뉘앙스 사전 — 비교군 중 유일하게 "왜 이 단어인가"를 다룸.
- 즉시 완성형 산출물 — `korean-report-skills`는 린팅만 하고 재작성은
  안 해줘서 사람이 다시 써야 함.
- 정부기관 서식 정합성 — `korean-itemization`은 전통기호(□/ㅇ/-)를
  배제하는 방향이라 오히려 정부 실무와 충돌.
- 실전 검증 이력 — 홍대거리 버스킹존 계획(안) 실제 문서에 적용해
  구조 중복·근거 없는 판단어를 실제로 잡아냄.
- 애자일한 개선 루프 — 코드가 아니라 문서라 리스크 없이 즉시 반영
  가능(이번 세션에만 서식 규칙·퇴고 감사·표 변환 기준 3건 추가).

**약점 및 우선순위**:
1. **판단어 검출의 재현성** — 퇴고 체크리스트에 감사 단계를 추가했지만
   `korean-report-skills`의 134개 규칙 기반 스캔과 달리 Claude의 판단에
   의존 — 매번 빠짐없이 잡아낸다는 기계적 보장이 없음. 다음에 실제
   보고서를 다듬을 때마다 이 감사 단계가 실제로 작동하는지 반복
   확인해야 함.
2. **검증 표본이 좁음** — 지금까지 검증된 문서 유형은 계획(안) 1건뿐.
   예산서·인사발령·국회답변·연설문 등 다른 장르에서는 아직 검증 안 됨.
   기회가 될 때마다 다양한 장르로 테스트를 넓혀야 함.
3. 자료수집 단계(8단계 중 1위, 33.3%) 미지원, 앱과 미연동, 엄격한
   구조 강제 없음(명사형 종결 통일 등)은 **의도적 설계 선택**이라
   당장 개선 과제로 보지 않음(위 "재정렬 불필요" 결론 참고).

## [2순위] 완료: 표 구조 정밀 조정 (C1/C2/C3) — 실제 한/글 검증 완료(2026-09-25)

> **검증 결과(1.68 Alpha 6, `표 서식.hwpx` 사본에 기능별로 따로 적용, 결과 XML과 쪽 이미지로 확인)**
> - **표 칸 묶음(공통)**: 'A1이 나오면 새 표' 규칙이 가운데 열이 세로 병합된 표를 세
>   조각으로 나눴다(표 10개로 셈, 실제 8개). 칸을 담은 표 컨트롤(`ParentCtrl`) 앵커로
>   묶도록 고침(`표칸_묶음_수집`) — `한칸표_영역_목록`(한 칸 표 판정)도 같은 기준 사용.
> - **C1 셀 안쪽 여백**: 동작함. 다만 1줄로 만들지 못한 칸도 여백이 0으로 남던 버그(46칸)
>   → 실패하면 원래 여백으로 되돌리도록 고침. 이제 1줄이 된 칸(2칸, 1.8→1.2mm)만 바뀐다.
> - **C2 열 너비 본문 맞춤**: 병합된 칸이 있는 표에서 너비가 엉뚱한 열에 들어가 표가
>   용지 밖으로 넘침(4쪽→7쪽) → 모든 행의 열 구성이 같은 격자형 표만 조정하도록 고침.
>   격자형 표는 비율대로 정확히 맞춰짐(약 +4.8%). **남은 위험**: 표 위에 겹쳐 놓은
>   그리기 개체(일정표의 기간 상자 등)는 표 너비가 바뀌어도 따라오지 않아 어긋나고,
>   쪽 수가 바뀔 수 있다(4→5쪽) — 기본값 꺼짐 유지를 권장.
> - **C3 삼선표 테두리**: 예전 코드(`pset.SelCellsBorderFill` 하위 항목)는 실행 결과가
>   True여도 **테두리가 전혀 바뀌지 않았다**. 셀을 `TableCellBlock`으로 선택한 뒤
>   `HCellBorderFill`의 `BorderType*`/`BorderWidth*`를 직접 넣고 `CellBorder` 액션을
>   실행하도록 고침 — 외곽 0.5mm·머리글 아래 이중선·좌우 외곽선 없음이 실제 적용되고
>   셀 배경색도 보존됨을 XML·이미지로 확인.
> - 세 기능 모두 기본값은 계속 꺼져 있다(C1·C3은 켜서 써도 되는 상태, C2는 주의).

C1(셀 안쪽 여백 축소)/C2(셀 너비 본문 맞춤)/C3(표 테두리 선 굵기 통일,
삼선표)는 `hwp-auto-docfit.py`에 구현 완료(표_셀_안쪽여백_전체_적용,
표_열너비_본문맞춤_전체_적용, 표_테두리_전체_적용 및 관련 헬퍼).
PyPI의 `pyhwpx`·`hwpapi` 패키지 소스(포럼 문서 사이트는 이 환경에서
접근 차단됨)를 읽어 확인한 API를 근거로 작성했으나, 이 환경에는 한/글이
없어 실제로 실행해 검증하지 못했다.

**기본값이 꺼져 있다**(표_셀여백_축소_사용 / 표_열너비_맞춤_사용 /
표_테두리_통일_사용 = False). 실제 Windows + 한/글에서 사본으로 먼저
확인한 뒤 켜서 쓰기를 권한다. 특히 C3(표_셀_테두리_적용 함수)의
`HCellBorderFill.SelCellsBorderFill.BorderTypeTop/Bottom/Left/Right`
같은 중첩 속성 경로는 세 기능 중 가장 확인 근거가 간접적이므로(hwpapi
소스의 필드 목록에 근거함, 직접 실행 예제는 없었음) 가장 먼저 의심해볼
부분이다.

## 다음에 진행할 작업

### [4순위] 최종검수 확장 — 숫자 데이터 일치 검사 (신규, 설계 착수 전)

**배경**: "실무 보고서 8단계 시간 비중" 검토에서 4위 단계(최종 검토 및
교정·교열, 8.3%)의 주요 작업으로 "본문 텍스트의 숫자와 표/그래프 수치
일치 여부 확인"이 명시적으로 꼽혔다. 지금 있는 최종검수
(`최종검수_실행`, `hwp-auto-docfit.py`)는 단어 중간 줄바꿈·문두기호 뒤
공백·마지막 줄 병합 대상만 재확인하고, 숫자 일치는 다루지 않는다.

**성격**: 콘텐츠를 새로 만들거나 판단하는 게 아니라 **이미 쓰인 두
곳의 숫자를 기계적으로 대조**하는 작업이라, "판단하지 않고 규칙으로
확인한다"는 이 앱 최종검수의 기존 성격과 맞고 A 방향(서식 자동화 심화,
낮은 리스크) 안에 들어온다.

**아직 없는 것 — 설계부터 필요**:
- 본문 문단 텍스트에서 숫자(금액·건수·비율·연도 등)를 추출하는 규칙.
  단위(원/명/건/%)와 자릿수 구분 쉼표까지 포함해 "숫자"로 볼지 결정
  필요.
- 표 안 셀 텍스트에서 대응하는 숫자를 추출하는 규칙(표는 이미
  `표_목록_수집()`으로 셀 단위 순회 기반이 있어 재사용 가능 — C1/C2/C3
  구현 때 만든 헬퍼).
- "본문의 이 숫자가 표의 어느 셀과 대응하는가"를 자동으로 짝짓는 방법이
  없다는 게 가장 큰 난제 — 완전 자동 매칭은 오탐(관계없는 숫자를 잘못
  짝지음)이나 미탐(실제 불일치를 놓침) 위험이 크다. 현실적인 범위는
  "본문에 등장한 숫자가 문서 어딘가(표 포함)에 최소 한 번은 똑같이
  나오는지"만 확인하는 **느슨한 존재 여부 검사** 정도로 시작하는 게
  안전해 보임(정확한 대응관계 검증이 아니라 "완전히 근거 없는 숫자는
  아닌지"의 1차 스크리닝).
- 오탐이 많으면 오히려 신뢰를 깎으므로, 최종검수 결과에 "확인 필요"로만
  표시하고 자동 수정은 하지 않는 현재 최종검수의 원칙(읽기 전용, 아무
  것도 고치지 않음)을 그대로 따라야 함.

**다음 단계 전 확인 필요**: 정확히 어떤 숫자 불일치를 잡고 싶은
것인지(같은 문서 안 본문-표 대조만? 여러 취합 문서 간 수치 정합성도?)
사용자 확인이 먼저 필요 — 범위에 따라 난이도가 크게 달라짐.

### [6순위] 서식 예시를 "직접 편집"으로 만드는 방법 추가 (새 문서 작성 → 분석 → 프로파일 등록)

**요청 배경**: 현재 서식 예시 추가는 기존 HWP/HWPX 파일을 드래그앤드롭하거나
"서식 복사하기" 대화상자로 파일을 골라야만 가능하다(`서식파일_드롭`,
`_서식_복사하기` → `_서식_분석_시작`, `hwp-auto-docfit.py:10743~10797`).
기관·목적별로 서식이 워낙 다양해서, 기존 파일 없이 사용자가 그 자리에서
"이런 서식이면 좋겠다"는 예시 문서를 직접 써서 등록하고 싶은 경우를
지원해야 한다.

**요구 흐름** (사용자 설명 원문 기준):
1. 설정창에 "서식 예시파일 편집하여 추가" 버튼 신설.
2. 버튼을 누르면 한/글 앱이 실행되고 빈 새 문서가 열림.
3. 사용자가 문서 내용을 직접 입력(문두기호 □/ㅇ/-/※ 등을 섞어 원하는
   계층 구조로 작성).
4. 편집이 끝나면 앱이 문서를 분석 — 문두기호 중심 계층 구조를 기준으로
   각 문서 요소(마커별 글꼴·크기·문장 앞 여백 등)를 집계.
5. 각 요소의 사용 횟수를 확률(빈도)로 환산해, 가장 높은 확률의 값을
   그 마커의 대표 서식값으로 확정.
6. 확정된 대표 서식을 적용한 예시 문서를 사용자에게 보여줌(미리보기/확인).
7. 사용자가 확인하면 새 서식 종류로 등록.
8. 사용자는 이후 언제든 서식 이름을 수정할 수 있어야 함.
9. 새로 추가되는 서식의 기본 이름 = 예시 문서 첫 문장 중 앞 8글자.

**이미 있어서 재사용 가능한 부분** (전체를 새로 만들 필요 없음):
- 빈도 기반 대표 서식값 산출: `docfit_core/style_hierarchy.py`의
  `analyze_hierarchy()`가 이미 마커별 들여쓰기·글꼴·크기를
  `Counter.most_common()`(`_most_common` 헬퍼)으로 집계해 최빈값을
  뽑고 있다. 요청한 "사용확률 계산 → 최고확률 요소를 대표값으로" 로직은
  본질적으로 이미 구현돼 있음 — 이번 요청은 "입력을 어디서 받는가"의
  새 경로 하나를 추가하는 것에 가깝다.
- 미리보기·확인 UI: `_서식_구조_확인(profile, parent)`
  (`hwp-auto-docfit.py:10833~`)가 "서식 구조 검토·수정" 대화상자로
  마커별 반복/비반복 스타일을 사용자에게 보여주고 수정할 기회를 준다 —
  요청한 6번(확인 후 추가) 단계와 거의 일치.
- 프로파일 저장: `_서식_복사완료()`가 `서식프로파일_폴더()`에 UUID
  파일명으로 JSON 저장, `self._프로파일들`/콤보박스 갱신까지 처리.

**새로 만들어야 하는 부분**:
1. "빈 새 문서로 한/글 열기" 버튼 + 그 새 한/글 세션을 앱이 계속 붙들고
   있는 방법. 기존 `텍스트_hwpx_단독변환()`(`hwp-auto-docfit.py` 부근)이
   쓰는 "배치 작업과 독립된 단독 hwp 세션"(`win32.Dispatch("HwpFrame.HwpObject")`
   를 별도로 띄우는 패턴)을 참고해, 이번엔 즉시 저장하지 않고 사용자가
   편집을 마칠 때까지 세션을 열어 둔 채 대기해야 함.
2. "편집 완료" 신호를 어떻게 받을지 결정 필요(열린 질문 — 아래 참고).
   결정되면: 그 시점의 문서를 임시 hwpx로 저장 → 기존
   `한글파일_서식_분석(path)`(`_서식_분석_시작`이 호출하는 바로 그 함수)에
   그대로 넘기면 나머지(집계·미리보기·저장) 파이프라인은 재사용 가능.
3. 기본 이름 로직 변경: 현재 `_서식_분석_시작`은 기본 이름을
   `Path(path).stem`(파일명)으로 쓴다(`hwp-auto-docfit.py:10754`). 이번
   요청대로면 파일명이 아니라 "문서 첫 문장의 앞 8글자"를 뽑아야 하므로,
   문서 텍스트에서 첫 문장을 판정하는 규칙(첫 문단 전체? 첫 마침표까지?
   문두기호는 포함/제외?)을 새로 정하고 이름 생성 함수를 추가해야 함.
   (기존 파일-드롭 경로의 기본 이름도 이 규칙으로 통일할지, 이번 새
   경로에만 적용할지는 사용자에게 확인 필요.)
4. "서식 이름 언제든 수정" 기능: 현재 저장된 프로파일의 이름을 나중에
   바꾸는 UI가 코드에 보이지 않는다(생성 시 `simpledialog.askstring`로
   한 번 정하거나 파일명을 그대로 쓸 뿐). 프로파일 목록(콤보박스/설정창)에
   "이름 바꾸기" 메뉴나 버튼을 새로 추가하고, `서식프로파일_폴더()`에 저장된
   해당 JSON의 `name` 필드만 갱신하는 함수를 만들어야 함.

**열린 질문** (구현 착수 전 사용자 확인 필요):
- "편집 완료"를 앱이 어떻게 알아채나 — (a) 앱에 "분석 시작" 버튼을 두고
  사용자가 직접 누르게 할지, (b) 한/글 문서의 저장(Ctrl+S) 이벤트를
  감지할지, (c) 한/글 창이 닫히는 시점을 감지할지.
- 사용자가 아직 문두기호를 하나도 쓰지 않은(구조 마커가 전혀 없는) 채로
  분석을 시도하면 어떻게 안내할지(빈 분석 결과 경고, 최소 요구 조건 안내 등).
- "첫 문장"의 정확한 범위 — 첫 문단 전체 vs 첫 마침표(.)까지, 문두기호
  자체는 8글자에 포함할지 제외할지.

### [3순위] 작업 모드를 "기본후처리용"/"전문후처리용"으로 재구성 + "서식통일" 기능 신설

> **완료(2026-09-25, 1.68 Alpha 6)** — 사용자 결정: 기존 3카드는 두고 위에 프리셋 2개 추가.
> - 실행창 '빠른 선택': **기본후처리**(자간 정리 + 서식통일), **전문후처리**(한 번에 정리 +
>   서식통일). 프리셋을 고르면 해당 카드와 서식통일이 켜지고, 카드를 직접 고르면 서식통일은 꺼진다.
> - 새 세부 작업 `style_unify`(서식통일): 세 모드 모두에 있으며 **기본 꺼짐(옵트인)**. 공백·문장부호
>   정리 뒤, 표준서식·자간 조정 앞에서 실행.
> - 서식통일 규칙(열린 질문의 결정): 대상은 문두기호 문단(□·ㅇ·-·*·※ 등)의 본문 글자(라벨 뒤)만 —
>   제목·날짜 같은 일반 문단과 표·글상자는 제외(의도된 스타일 차이 보존). 기호별로 글꼴·크기·장평을
>   모아 같은 기호 문단 3개 이상·대표 비율 60% 이상일 때만 대표로 정하고, 다른 문단의 다른 항목만
>   바꾼다(굵기·라벨 유지). 한 문단 안에서 첫 글자와 가운데 글자가 다르면 건너뛴다.
> - 실제 한/글 검증: 서식적용 테스트 문서 사본에서 ㅇ 문단 2개를 굴림 12pt로 바꾼 뒤 실행 →
>   대표(한컴돋움 15pt, 20개 중 18개)로 2개만 되돌아옴, □·- 문단은 0개 교정.

**요청 원문 요지**: 지금 실행창의 3카드(자간 정리/서식 정리/한 번에 정리,
`hwp-auto-docfit.py:9496~9498`, `self.selected_mode` = spacing/format/all)를
두 단계로 다시 묶는다.
- **기본후처리용** = 자간정리 + 서식통일
- **전문후처리용** = 자간정리 + 서식통일 + 서식적용 + 한번에 적용

여기서 "서식적용"은 기존 "서식 정리"(format), "한번에 적용"은 기존
"한 번에 정리"(all)에 대응하는 것으로 보임(명칭만 다듬은 것인지, 의미
자체가 달라지는지는 확인 필요). **"서식통일"은 기존에 없던 완전히 새
기능**이다.

**서식통일 기능 정의** (사용자 설명 원문): 여러 문서를 취합해 하나로 만들
때 작성자별 실수로 스타일이 일관되지 않은 경우가 있다. 이 문서(취합된
하나의 문서) 안에서 **스타일 요소별 사용 빈도 확률**을 기초로 대표
스타일을 파악하고, 대표 스타일과 다른("오류가 있는") 스타일을 대표
스타일로 교정해 문서 전체에 하나의 스타일만 유지되게 한다.

**이 기능은 이미 이 세션 초반 "B3"로 설계까지 논의된 적이 있다** — 원문:
"복사·붙여넣기 후 서식 깨짐 복원: 문서에 적용된 스타일과 다르게 이질적인
스타일은 문서에서 가장 높은 비중으로 사용되는 스타일을 제1 스타일로
간주하여 이질적인 스타일을 교정하여 문서 전체적으로 하나의 스타일이
통일되게 적용되도록 하는 기능." 그때 검토한 구현 방향을 그대로 이어서
쓸 수 있다:
1. 처리 중인 문서를 임시 HWPX로 스냅샷 저장(`hwp.SaveAs(temp, "HWPX", "")`).
2. 바로 위 항목("서식 예시 직접 편집 추가")에서도 재사용을 언급한
   `hwpx_서식_분석(path)`/`analyze_hierarchy()`를 **그 스냅샷 자체에
   대해** 실행 — 외부 예시 문서가 아니라 "지금 처리 중인 문서 자신"을
   분석 대상으로 삼는다는 점이 서식 예시 기능과의 유일한 차이. 마커별
   글꼴·크기·여백의 최빈값(`Counter.most_common`)이 곧 "대표 스타일".
3. `_프로파일_전역반영(profile)`(`hwp-auto-docfit.py:9821`)이 이미
   프로파일 dict를 받아 `표준서식_설정`/`표_헤더서식_*`/
   `활성_정밀표_프로필` 전역을 덮어쓰는 로직을 갖고 있다 — 다만 이건
   GUI 스레드 메서드라 배치 처리 중인 워커 스레드에서 그대로 호출할 수
   없다. self 없이 순수 함수로 뽑아내 워커 스레드에서 써야 한다.
4. 그 문서 처리가 끝나면(다음 문서로 넘어가기 전) 전역 상태를 원래
   값으로 복원해야 한다 — 안 그러면 배치의 다른 문서까지 방금 분석한
   임시 대표 스타일의 영향을 받는다.
5. 이 기능은 "고정된 기준 서식을 적용"하는 기존 서식 정리와 근본적으로
   달라서, 옵트인 토글(체크박스)로 노출해야 한다 — 기준이 "내가 고른
   예시/프로파일"이 아니라 "이 문서 자체에서 다수인 스타일"로 바뀌는
   것이기 때문.

**모드 재구성 관련 열린 질문**:
- "기본"/"전문" 두 단계로 나누는 게 기존 3카드 UI를 완전히 대체하는
  것인지, 아니면 3카드 위에 "이 조합으로 한 번에" 실행하는 프리셋
  버튼 2개를 추가하는 것인지 확인 필요.
- 서식통일을 "자간정리" 뒤/"서식적용" 앞 등 어느 단계에서 실행할지
  (문서_처리_1회의 스테이지 순서 중 어디에 끼울지) — 서식통일이
  뒤바꾼 표준서식 기준값을 자간·내어쓰기 단계가 참고해야 하므로
  순서가 중요함.
- 서식통일이 예외로 봐야 할 것(예: 표 헤더처럼 본문과 의도적으로
  다른 스타일)을 어떻게 구분할지 — 무조건 최빈값으로 통일하면 의도된
  스타일 차이까지 지워버릴 위험이 있음.

### [5순위] 아웃라이너 기능 강화 (Workflowy 벤치마킹)

**현재 상태**: '01 정리할 문서'의 "아웃라이너로 작성" 버튼(`_아웃라이너_열기`,
`hwp-auto-docfit.py:10834~`)이 이미 워크플로위를 본떠 만들어져 있다.
Tk `Text` 위젯 두 칸(아웃라인 입력 / 공문서 개조식 미리보기)으로 구성돼
있고, 지금 지원하는 것은:
- Enter로 같은 계층에 새 항목, Tab/Shift+Tab으로 들여쓰기/내어쓰기
  (`엔터처리`/`들여쓰기`/`내어쓰기` 클로저, 같은 함수 안).
- 0단계 "# ", 그 아래는 들여쓰기 깊이만큼 "- "를 겹친 마크다운으로 저장.
- "개조식 텍스트로 추가"(0/1/2/3단계 이후를 각각 ㅁ/ㅇ/-/•로 변환해
  문서 목록에 추가) 또는 "마크다운으로 추가"(원본 그대로 추가, 고급
  문서 엔진 kordoc이 있으면 서식을 살려 변환).
- 붙여넣기 정리 쪽에서도 워크플로위를 브라우저에서 통째로 복사해 온
  경우(줄바꿈이 공백으로 뭉개진 경우)를 인식해 해시태그(#표시)까지
  함께 정리해 주는 보조 기능이 있음(항목 50).

**확인된 한계 — 지금 없는 것** (실제 워크플로위와 비교):
- 접기/펼치기(collapse) 없음 — 항목이 많아지면 전체가 한 화면에 계속
  펼쳐져 있어야 함.
- 확대(zoom-in, 특정 항목만 화면 최상단으로 올려 그 하위만 집중해서
  편집) 없음.
- 항목 이동(드래그 또는 단축키로 위/아래 순서 바꾸기, 다른 상위 항목
  밑으로 옮기기) 없음 — 순서를 바꾸려면 직접 잘라 붙여야 함.
- 검색/필터 없음.
- 완료 표시(체크박스·취소선) 없음.
- 항목별 메모(note, 제목 줄과 별도로 붙는 보조 설명 한두 줄) 없음.
- 태그(#태그) 입력·자동완성 없음 — 붙여넣기 정리에서 "인식"만 하고,
  이 아웃라이너 자체에서 입력하는 기능은 없음.
- 미러(같은 항목을 여러 위치에 복제해 두면 한쪽 수정 시 같이 바뀜)
  없음 — 우선순위 낮은 고급 기능으로 분류.
- 저장된 아웃라인을 나중에 다시 열어 이어 쓰는 기능 없음 — 창을 열
  때마다 빈 문서로 시작하고, "추가" 버튼을 누르면 파일로 저장 후 바로
  문서 목록에 등록되는 1회성 작성 도구에 가깝다. 이어 쓰기를 하려면
  저장된 .md 파일을 파일 탐색기에서 다시 열어 붙여넣는 수밖에 없음.

**다음 단계 전 확인 필요**: 위 목록 중 실제로 강화하고 싶은 항목(전부는
아닐 가능성이 큼)을 사용자에게 우선순위로 받아야 함. 특히 "이어 쓰기
(저장된 아웃라인 다시 열기)"는 이 도구의 "1회성 작성 후 바로 내보내기"
설계 전제 자체를 바꾸는 큰 변경이라 별도로 다시 논의가 필요해 보임.

### [7순위, 재검토 필요] 윈도우 스티커 메모 연동

**A 방향 확정에 따른 재검토**: 8단계 시간 비중 어디에도 명확히 걸리지
않는 아이디어다. A(서식 자동화 심화) 기준으로 우선순위를 매기면 가장
낮다 — 착수 전에 "왜 필요한지"(어느 업무 단계를 줄여주는지)부터 다시
확인 필요.

**아이디어만 있고 구체 요구사항은 아직 없음** — 방향(스티커 메모 →
이 앱, 이 앱 → 스티커 메모, 또는 양방향)조차 확인되지 않았다. 다만
기술적으로 조사해 둔 사실:
- Windows 10/11의 스티커 메모(UWP 앱)는 메모를 텍스트 파일이 아니라
  SQLite DB(`%LocalAppData%\Packages\Microsoft.MicrosoftStickyNotes_8wekyb3d8bbwe\LocalState\plum.sqlite`)
  에 저장한다. `Note` 테이블에 메모 내용이 들어 있고(정확한 스키마·
  서식 인코딩은 구현 시 실 파일로 직접 확인 필요), 트랜잭션 로그가
  같은 폴더의 `plum.sqlite-shm`/`plum.sqlite-wal`에 남는다.
- 공식 API는 없고, 포렌식 도구들(예: StickyParser)이 이 sqlite 파일을
  직접 읽는 방식으로 동작한다 — 이 앱도 같은 방식(`sqlite3` 표준
  라이브러리로 직접 읽기)이 유일한 현실적 경로로 보임.

**다음 단계 전 확인 필요**: 무엇을 하고 싶은 기능인지부터 정해야 함 —
예를 들어 (a) 스티커 메모에 적어 둔 개조식 메모를 이 앱의 "정리할
문서" 목록에 바로 추가하는 기능인지, (b) 이 앱이 처리한 결과 요약을
스티커 메모로 남기는 기능인지, (c) 완전히 다른 용도인지에 따라 필요한
작업이 크게 달라짐.
