# 작업 예정 목록

지금 당장 진행하지 않고 미뤄둔 기능 요청을 적어둔다. "나중에 작업해"라고
명시적으로 요구받으면 그때 해당 항목을 꺼내 구현한다.

## 확정된 개발 방향: A (서식 자동화 심화)

실무 보고서(5~10쪽, 총 10시간 기준) 작성 8단계의 시간 비중 데이터를
검토한 뒤 사용자가 확정: **A안(서식 자동화 심화 — 리스크 낮음, 상한
명확)**으로 간다. B안(자료 수집·본문 작성까지 AI가 관여하는 콘텐츠
지원으로 확장, 상한 훨씬 높지만 리스크·정체성 변화 큼)은 채택하지
않음.

이에 따라 "다음에 진행할 작업"의 우선순위를, A안이 다루는 3위(서식
적용·시각화 13.3%)·4위(최종 검토·교정 8.3%)·5위(쪽 맞추기 6.7%)·
6위(자간·행간 미세조정 5.8%) 단계에 대한 기여도를 기준으로 재정렬한다.
숫자는 "실무 보고서 8단계 시간 비중 검토" 보고에서 인용.

1. **C1/C2/C3(표 구조 정밀 조정) 실 한/글 검증** — 3위 단계의 "표 작성"
   부분에 직접 기여, 이미 구현은 끝났고 검증만 남음(가장 빠르게 가치를
   확인할 수 있음).
2. **서식통일 기능 신설**(+ 작업모드 재구성) — 3위(서식 일관성)와
   4위(교정·교열 중 스타일 오류 확인)에 걸침. 지금 앱에 없는 새 능력.
3. **최종검수 확장 — 숫자 데이터 일치 검사** — 4위 단계가 명시한 핵심
   작업("본문 텍스트의 숫자와 표/그래프 수치 일치 여부 확인")을 정면으로
   겨냥. 다만 아직 설계 착수 전(신규 항목, 아래 참고).
4. **아웃라이너 기능 강화** — 7위(목차 구성, 4.2%)에 기여, 비중이 작아
   후순위.
5. **서식 예시 "직접 편집" 추가** — 3위 단계를 위한 준비 작업(서식 등록)을
   편하게 하는 UX 개선. 반복 작업 자체를 줄이진 않아 후순위.
6. **윈도우 스티커 메모 연동** — 8단계 시간 비중 어디에도 명확히
   대응되지 않음. A 방향의 우선순위 기준으로는 근거가 약하니, 착수 전에
   "왜 필요한지"부터 다시 확인.

(자료 수집·본문 초안 작성을 돕는 "보고서 작성 스킬" 논의는 B안 성격이라
A안 확정 이후 보류였으나, 사용자가 완성된 Claude Skill 패키지
(`gongmunseo-report-writing`, 「칼퇴를 위하여」 1~5권 기반)를 직접 만들어
전달함에 따라 `.claude/skills/gongmunseo-report-writing/`에 추가 완료.
이 앱(hwp-auto-docfit) 코드와는 무관한 사용자 개인 작성 보조 스킬이며,
A안 우선순위 목록과는 별개로 취급한다.

스킬 추가 후 "이 스킬을 포함해 우선순위를 다시 검토해야 하는가"를
재검토했고, 결론은 **재정렬 불필요**. 두 도구는 실행 표면이 달라 시간
비중을 단순 합산할 수 없다 — 앱은 HWP 파일에 무인 일괄 자동화를
적용해 해당 단계 시간을 거의 0으로 만들지만(그래서 A안의 "리스크
낮음·상한 명확"이 성립), 스킬은 Claude와의 대화 속에서 문체·구조를
조언할 뿐 최종 집필은 사람이 한다. 스킬이 커버하는 2위(본문작성)·
4위(교정) 비중을 앱의 25.8%에 더해 "이제 대부분 해결됐다"고 보면 안
됨. 특히 1위 단계(자료수집·검증, 33.3%, 8단계 중 최대)는 스킬도 전혀
다루지 않는 영역으로 여전히 완전한 공백이며, 이는 사실 검색·출처
신뢰성 검증이라는 스킬의 범위(문체·논리 정리) 밖의 별개 문제라 지금
단계에서는 손대지 않는다.

교차 발견 하나: 앱의 **[3순위] 숫자 데이터 일치 검사**(본문 텍스트
숫자 ↔ 표 수치 기계적 대조, 4위 단계 겨냥)와 스킬의 6단계 "퇴고"가
개념적으로 같은 문제를 겨냥한다. 스킬 쪽 `SKILL.md` 퇴고 체크리스트에
숫자 대조 항목을 추가 완료(저비용 문서 수정이라 앱 우선순위와 무관하게
바로 반영). 앱 쪽 [3순위] 항목은 기계적·결정론적 대조라는 점에서 여전히
독립된 가치가 있어 그대로 유지한다.

**유사 스킬·앱 조사 결과(웹 검색, 2026-09-25):** 동일한 결 — 「칼퇴를
위하여」 기반 어휘 뉘앙스 사전 + A+B 문장구조 철학을 갖춘 도구는 없음.
다만 "개조식 문서 작성 보조"라는 목적 자체는 이미 여러 도구가 다룸.
- **가장 근접**: `NAMYUNWOO/korean-itemization` (GitHub, MIT) — 개조식
  6원칙(한 항목 한 사실, 두괄식+키워드, 명사형 종결, 중복 제거, 병렬성,
  위계)을 가이드하는 Claude 스킬. 다만 형식·문장종결 규칙 중심이고,
  어휘 뉘앙스 사전이나 "왜 이 단어를 고르는가" 층위는 없음. 저자명이
  「칼퇴를 위하여」 저자 남윤철과 발음이 비슷하나 동일인 여부 불명,
  내용상 그 책을 인용하지 않아 무관해 보임.
- `JangHyun-bin/korean-report-skills` — 134개 치환 규칙 기반 린팅(번역투·
  모호한 수치·근거 없는 주장 검출) + HTML/PDF 출력 디자인. 검수·시각적
  출력에 방점, 방법론(규칙 기반 린터)이 우리 스킬과 다름.
- `korean-business-document-generator`(mcpmarket) — 민간기업 주간보고·
  기안·품의·회의록 대상, 결재라인 구조화. 정부기관 개조식이 아니라
  회사 격식체 초점.
- 범용 AI 보고서 도구(한컴어시스턴트, ChatGPT GPTs 등)는 뉘앙스 사전이나
  A+B 구조 근거 제시 없이 "그냥 써준다"는 접근이라 결이 다름.
- (참고, 스킬이 아니라 앱 쪽 비교군) **범정부오피스** — 행안부가 2023년
  배포한 무료 공문서 서식 자동화 프로그램, 20여 개 기관 도입, 1,000여
  기능(폰트·글머리·간격 일괄 정리, 어절 잘림 방지 자동 자간, AI 초안
  생성 등). 이건 `gongmunseo-report-writing`(문체 스킬)보다
  `hwp-auto-docfit`(서식 자동화 앱) 쪽과 기능이 크게 겹쳐, 앱 로드맵
  검토 시 참고할 실사용 경쟁 사례로 별도 기록해둔다.

결론: 지금 스킬을 교체하거나 저 스킬들을 가져다 쓸 필요는 없음(뉘앙스
사전 + 원저자 철학이라는 차별점이 뚜렷함). 다만 `korean-itemization`의
형식 규칙(명사형 종결, 위계 nesting 제한 등)은 우리 `formatting.md`가
아직 다루지 않는 부분이 있는지 향후 한 번 대조해볼 가치는 있음.)

## 스킬 경쟁력 평가 (`gongmunseo-report-writing`, 2026-09-25)

**핵심 경쟁력**: "근거 있는 뉘앙스 판단 + 즉시 완성형 산출물 + 정부
서식 정합성"의 조합. 비교군(`korean-itemization`,
`korean-report-skills`, `korean-business-document-generator`, 범용 AI
보고서 도구) 중 이 세 가지를 동시에 갖춘 도구는 없음.

**강점**:
- 어휘 뉘앙스 사전 — 비교군 중 유일하게 "왜 이 단어인가"를 다룸.
- 즉시 완성형 산출물 — `korean-report-skills`는 린팅만 하고 재작성은
  안 해줘서 사람이 다시 써야 함.
- 정부기관 서식 정합성 — `korean-itemization`은 전통기호(□/ㅇ/-)를
  배제하는 방향이라 오히려 정부 실무와 충돌.
- 실전 검증 이력 — 홍대거리 버스킹존 계획(안) 실제 문서에 적용해
  구조 중복·근거 없는 판단어를 실제로 잡아냄.
- 애자일한 개선 루프 — 코드가 아니라 문서라 리스크 없이 즉시 반영
  가능(이번 세션에만 서식 규칙·퇴고 감사·표 변환 기준 3건 추가).

**약점 및 우선순위**:
1. **판단어 검출의 재현성** — 퇴고 체크리스트에 감사 단계를 추가했지만
   `korean-report-skills`의 134개 규칙 기반 스캔과 달리 Claude의 판단에
   의존 — 매번 빠짐없이 잡아낸다는 기계적 보장이 없음. 다음에 실제
   보고서를 다듬을 때마다 이 감사 단계가 실제로 작동하는지 반복
   확인해야 함.
2. **검증 표본이 좁음** — 지금까지 검증된 문서 유형은 계획(안) 1건뿐.
   예산서·인사발령·국회답변·연설문 등 다른 장르에서는 아직 검증 안 됨.
   기회가 될 때마다 다양한 장르로 테스트를 넓혀야 함.
3. 자료수집 단계(8단계 중 1위, 33.3%) 미지원, 앱과 미연동, 엄격한
   구조 강제 없음(명사형 종결 통일 등)은 **의도적 설계 선택**이라
   당장 개선 과제로 보지 않음(위 "재정렬 불필요" 결론 참고).

## [1순위] 완료: 표 구조 정밀 조정 (C1/C2/C3) — 실제 한/글 검증 필요

C1(셀 안쪽 여백 축소)/C2(셀 너비 본문 맞춤)/C3(표 테두리 선 굵기 통일,
삼선표)는 `hwp-auto-docfit.py`에 구현 완료(표_셀_안쪽여백_전체_적용,
표_열너비_본문맞춤_전체_적용, 표_테두리_전체_적용 및 관련 헬퍼).
PyPI의 `pyhwpx`·`hwpapi` 패키지 소스(포럼 문서 사이트는 이 환경에서
접근 차단됨)를 읽어 확인한 API를 근거로 작성했으나, 이 환경에는 한/글이
없어 실제로 실행해 검증하지 못했다.

**기본값이 꺼져 있다**(표_셀여백_축소_사용 / 표_열너비_맞춤_사용 /
표_테두리_통일_사용 = False). 실제 Windows + 한/글에서 사본으로 먼저
확인한 뒤 켜서 쓰기를 권한다. 특히 C3(표_셀_테두리_적용 함수)의
`HCellBorderFill.SelCellsBorderFill.BorderTypeTop/Bottom/Left/Right`
같은 중첩 속성 경로는 세 기능 중 가장 확인 근거가 간접적이므로(hwpapi
소스의 필드 목록에 근거함, 직접 실행 예제는 없었음) 가장 먼저 의심해볼
부분이다.

## 다음에 진행할 작업

### [3순위] 최종검수 확장 — 숫자 데이터 일치 검사 (신규, 설계 착수 전)

**배경**: "실무 보고서 8단계 시간 비중" 검토에서 4위 단계(최종 검토 및
교정·교열, 8.3%)의 주요 작업으로 "본문 텍스트의 숫자와 표/그래프 수치
일치 여부 확인"이 명시적으로 꼽혔다. 지금 있는 최종검수
(`최종검수_실행`, `hwp-auto-docfit.py`)는 단어 중간 줄바꿈·문두기호 뒤
공백·마지막 줄 병합 대상만 재확인하고, 숫자 일치는 다루지 않는다.

**성격**: 콘텐츠를 새로 만들거나 판단하는 게 아니라 **이미 쓰인 두
곳의 숫자를 기계적으로 대조**하는 작업이라, "판단하지 않고 규칙으로
확인한다"는 이 앱 최종검수의 기존 성격과 맞고 A 방향(서식 자동화 심화,
낮은 리스크) 안에 들어온다.

**아직 없는 것 — 설계부터 필요**:
- 본문 문단 텍스트에서 숫자(금액·건수·비율·연도 등)를 추출하는 규칙.
  단위(원/명/건/%)와 자릿수 구분 쉼표까지 포함해 "숫자"로 볼지 결정
  필요.
- 표 안 셀 텍스트에서 대응하는 숫자를 추출하는 규칙(표는 이미
  `표_목록_수집()`으로 셀 단위 순회 기반이 있어 재사용 가능 — C1/C2/C3
  구현 때 만든 헬퍼).
- "본문의 이 숫자가 표의 어느 셀과 대응하는가"를 자동으로 짝짓는 방법이
  없다는 게 가장 큰 난제 — 완전 자동 매칭은 오탐(관계없는 숫자를 잘못
  짝지음)이나 미탐(실제 불일치를 놓침) 위험이 크다. 현실적인 범위는
  "본문에 등장한 숫자가 문서 어딘가(표 포함)에 최소 한 번은 똑같이
  나오는지"만 확인하는 **느슨한 존재 여부 검사** 정도로 시작하는 게
  안전해 보임(정확한 대응관계 검증이 아니라 "완전히 근거 없는 숫자는
  아닌지"의 1차 스크리닝).
- 오탐이 많으면 오히려 신뢰를 깎으므로, 최종검수 결과에 "확인 필요"로만
  표시하고 자동 수정은 하지 않는 현재 최종검수의 원칙(읽기 전용, 아무
  것도 고치지 않음)을 그대로 따라야 함.

**다음 단계 전 확인 필요**: 정확히 어떤 숫자 불일치를 잡고 싶은
것인지(같은 문서 안 본문-표 대조만? 여러 취합 문서 간 수치 정합성도?)
사용자 확인이 먼저 필요 — 범위에 따라 난이도가 크게 달라짐.

### [5순위] 서식 예시를 "직접 편집"으로 만드는 방법 추가 (새 문서 작성 → 분석 → 프로파일 등록)

**요청 배경**: 현재 서식 예시 추가는 기존 HWP/HWPX 파일을 드래그앤드롭하거나
"서식 복사하기" 대화상자로 파일을 골라야만 가능하다(`서식파일_드롭`,
`_서식_복사하기` → `_서식_분석_시작`, `hwp-auto-docfit.py:10743~10797`).
기관·목적별로 서식이 워낙 다양해서, 기존 파일 없이 사용자가 그 자리에서
"이런 서식이면 좋겠다"는 예시 문서를 직접 써서 등록하고 싶은 경우를
지원해야 한다.

**요구 흐름** (사용자 설명 원문 기준):
1. 설정창에 "서식 예시파일 편집하여 추가" 버튼 신설.
2. 버튼을 누르면 한/글 앱이 실행되고 빈 새 문서가 열림.
3. 사용자가 문서 내용을 직접 입력(문두기호 □/ㅇ/-/※ 등을 섞어 원하는
   계층 구조로 작성).
4. 편집이 끝나면 앱이 문서를 분석 — 문두기호 중심 계층 구조를 기준으로
   각 문서 요소(마커별 글꼴·크기·문장 앞 여백 등)를 집계.
5. 각 요소의 사용 횟수를 확률(빈도)로 환산해, 가장 높은 확률의 값을
   그 마커의 대표 서식값으로 확정.
6. 확정된 대표 서식을 적용한 예시 문서를 사용자에게 보여줌(미리보기/확인).
7. 사용자가 확인하면 새 서식 종류로 등록.
8. 사용자는 이후 언제든 서식 이름을 수정할 수 있어야 함.
9. 새로 추가되는 서식의 기본 이름 = 예시 문서 첫 문장 중 앞 8글자.

**이미 있어서 재사용 가능한 부분** (전체를 새로 만들 필요 없음):
- 빈도 기반 대표 서식값 산출: `docfit_core/style_hierarchy.py`의
  `analyze_hierarchy()`가 이미 마커별 들여쓰기·글꼴·크기를
  `Counter.most_common()`(`_most_common` 헬퍼)으로 집계해 최빈값을
  뽑고 있다. 요청한 "사용확률 계산 → 최고확률 요소를 대표값으로" 로직은
  본질적으로 이미 구현돼 있음 — 이번 요청은 "입력을 어디서 받는가"의
  새 경로 하나를 추가하는 것에 가깝다.
- 미리보기·확인 UI: `_서식_구조_확인(profile, parent)`
  (`hwp-auto-docfit.py:10833~`)가 "서식 구조 검토·수정" 대화상자로
  마커별 반복/비반복 스타일을 사용자에게 보여주고 수정할 기회를 준다 —
  요청한 6번(확인 후 추가) 단계와 거의 일치.
- 프로파일 저장: `_서식_복사완료()`가 `서식프로파일_폴더()`에 UUID
  파일명으로 JSON 저장, `self._프로파일들`/콤보박스 갱신까지 처리.

**새로 만들어야 하는 부분**:
1. "빈 새 문서로 한/글 열기" 버튼 + 그 새 한/글 세션을 앱이 계속 붙들고
   있는 방법. 기존 `텍스트_hwpx_단독변환()`(`hwp-auto-docfit.py` 부근)이
   쓰는 "배치 작업과 독립된 단독 hwp 세션"(`win32.Dispatch("HwpFrame.HwpObject")`
   를 별도로 띄우는 패턴)을 참고해, 이번엔 즉시 저장하지 않고 사용자가
   편집을 마칠 때까지 세션을 열어 둔 채 대기해야 함.
2. "편집 완료" 신호를 어떻게 받을지 결정 필요(열린 질문 — 아래 참고).
   결정되면: 그 시점의 문서를 임시 hwpx로 저장 → 기존
   `한글파일_서식_분석(path)`(`_서식_분석_시작`이 호출하는 바로 그 함수)에
   그대로 넘기면 나머지(집계·미리보기·저장) 파이프라인은 재사용 가능.
3. 기본 이름 로직 변경: 현재 `_서식_분석_시작`은 기본 이름을
   `Path(path).stem`(파일명)으로 쓴다(`hwp-auto-docfit.py:10754`). 이번
   요청대로면 파일명이 아니라 "문서 첫 문장의 앞 8글자"를 뽑아야 하므로,
   문서 텍스트에서 첫 문장을 판정하는 규칙(첫 문단 전체? 첫 마침표까지?
   문두기호는 포함/제외?)을 새로 정하고 이름 생성 함수를 추가해야 함.
   (기존 파일-드롭 경로의 기본 이름도 이 규칙으로 통일할지, 이번 새
   경로에만 적용할지는 사용자에게 확인 필요.)
4. "서식 이름 언제든 수정" 기능: 현재 저장된 프로파일의 이름을 나중에
   바꾸는 UI가 코드에 보이지 않는다(생성 시 `simpledialog.askstring`로
   한 번 정하거나 파일명을 그대로 쓸 뿐). 프로파일 목록(콤보박스/설정창)에
   "이름 바꾸기" 메뉴나 버튼을 새로 추가하고, `서식프로파일_폴더()`에 저장된
   해당 JSON의 `name` 필드만 갱신하는 함수를 만들어야 함.

**열린 질문** (구현 착수 전 사용자 확인 필요):
- "편집 완료"를 앱이 어떻게 알아채나 — (a) 앱에 "분석 시작" 버튼을 두고
  사용자가 직접 누르게 할지, (b) 한/글 문서의 저장(Ctrl+S) 이벤트를
  감지할지, (c) 한/글 창이 닫히는 시점을 감지할지.
- 사용자가 아직 문두기호를 하나도 쓰지 않은(구조 마커가 전혀 없는) 채로
  분석을 시도하면 어떻게 안내할지(빈 분석 결과 경고, 최소 요구 조건 안내 등).
- "첫 문장"의 정확한 범위 — 첫 문단 전체 vs 첫 마침표(.)까지, 문두기호
  자체는 8글자에 포함할지 제외할지.

### [2순위] 작업 모드를 "기본후처리용"/"전문후처리용"으로 재구성 + "서식통일" 기능 신설

**요청 원문 요지**: 지금 실행창의 3카드(자간 정리/서식 정리/한 번에 정리,
`hwp-auto-docfit.py:9496~9498`, `self.selected_mode` = spacing/format/all)를
두 단계로 다시 묶는다.
- **기본후처리용** = 자간정리 + 서식통일
- **전문후처리용** = 자간정리 + 서식통일 + 서식적용 + 한번에 적용

여기서 "서식적용"은 기존 "서식 정리"(format), "한번에 적용"은 기존
"한 번에 정리"(all)에 대응하는 것으로 보임(명칭만 다듬은 것인지, 의미
자체가 달라지는지는 확인 필요). **"서식통일"은 기존에 없던 완전히 새
기능**이다.

**서식통일 기능 정의** (사용자 설명 원문): 여러 문서를 취합해 하나로 만들
때 작성자별 실수로 스타일이 일관되지 않은 경우가 있다. 이 문서(취합된
하나의 문서) 안에서 **스타일 요소별 사용 빈도 확률**을 기초로 대표
스타일을 파악하고, 대표 스타일과 다른("오류가 있는") 스타일을 대표
스타일로 교정해 문서 전체에 하나의 스타일만 유지되게 한다.

**이 기능은 이미 이 세션 초반 "B3"로 설계까지 논의된 적이 있다** — 원문:
"복사·붙여넣기 후 서식 깨짐 복원: 문서에 적용된 스타일과 다르게 이질적인
스타일은 문서에서 가장 높은 비중으로 사용되는 스타일을 제1 스타일로
간주하여 이질적인 스타일을 교정하여 문서 전체적으로 하나의 스타일이
통일되게 적용되도록 하는 기능." 그때 검토한 구현 방향을 그대로 이어서
쓸 수 있다:
1. 처리 중인 문서를 임시 HWPX로 스냅샷 저장(`hwp.SaveAs(temp, "HWPX", "")`).
2. 바로 위 항목("서식 예시 직접 편집 추가")에서도 재사용을 언급한
   `hwpx_서식_분석(path)`/`analyze_hierarchy()`를 **그 스냅샷 자체에
   대해** 실행 — 외부 예시 문서가 아니라 "지금 처리 중인 문서 자신"을
   분석 대상으로 삼는다는 점이 서식 예시 기능과의 유일한 차이. 마커별
   글꼴·크기·여백의 최빈값(`Counter.most_common`)이 곧 "대표 스타일".
3. `_프로파일_전역반영(profile)`(`hwp-auto-docfit.py:9821`)이 이미
   프로파일 dict를 받아 `표준서식_설정`/`표_헤더서식_*`/
   `활성_정밀표_프로필` 전역을 덮어쓰는 로직을 갖고 있다 — 다만 이건
   GUI 스레드 메서드라 배치 처리 중인 워커 스레드에서 그대로 호출할 수
   없다. self 없이 순수 함수로 뽑아내 워커 스레드에서 써야 한다.
4. 그 문서 처리가 끝나면(다음 문서로 넘어가기 전) 전역 상태를 원래
   값으로 복원해야 한다 — 안 그러면 배치의 다른 문서까지 방금 분석한
   임시 대표 스타일의 영향을 받는다.
5. 이 기능은 "고정된 기준 서식을 적용"하는 기존 서식 정리와 근본적으로
   달라서, 옵트인 토글(체크박스)로 노출해야 한다 — 기준이 "내가 고른
   예시/프로파일"이 아니라 "이 문서 자체에서 다수인 스타일"로 바뀌는
   것이기 때문.

**모드 재구성 관련 열린 질문**:
- "기본"/"전문" 두 단계로 나누는 게 기존 3카드 UI를 완전히 대체하는
  것인지, 아니면 3카드 위에 "이 조합으로 한 번에" 실행하는 프리셋
  버튼 2개를 추가하는 것인지 확인 필요.
- 서식통일을 "자간정리" 뒤/"서식적용" 앞 등 어느 단계에서 실행할지
  (문서_처리_1회의 스테이지 순서 중 어디에 끼울지) — 서식통일이
  뒤바꾼 표준서식 기준값을 자간·내어쓰기 단계가 참고해야 하므로
  순서가 중요함.
- 서식통일이 예외로 봐야 할 것(예: 표 헤더처럼 본문과 의도적으로
  다른 스타일)을 어떻게 구분할지 — 무조건 최빈값으로 통일하면 의도된
  스타일 차이까지 지워버릴 위험이 있음.

### [4순위] 아웃라이너 기능 강화 (Workflowy 벤치마킹)

**현재 상태**: '01 정리할 문서'의 "아웃라이너로 작성" 버튼(`_아웃라이너_열기`,
`hwp-auto-docfit.py:10834~`)이 이미 워크플로위를 본떠 만들어져 있다.
Tk `Text` 위젯 두 칸(아웃라인 입력 / 공문서 개조식 미리보기)으로 구성돼
있고, 지금 지원하는 것은:
- Enter로 같은 계층에 새 항목, Tab/Shift+Tab으로 들여쓰기/내어쓰기
  (`엔터처리`/`들여쓰기`/`내어쓰기` 클로저, 같은 함수 안).
- 0단계 "# ", 그 아래는 들여쓰기 깊이만큼 "- "를 겹친 마크다운으로 저장.
- "개조식 텍스트로 추가"(0/1/2/3단계 이후를 각각 ㅁ/ㅇ/-/•로 변환해
  문서 목록에 추가) 또는 "마크다운으로 추가"(원본 그대로 추가, 고급
  문서 엔진 kordoc이 있으면 서식을 살려 변환).
- 붙여넣기 정리 쪽에서도 워크플로위를 브라우저에서 통째로 복사해 온
  경우(줄바꿈이 공백으로 뭉개진 경우)를 인식해 해시태그(#표시)까지
  함께 정리해 주는 보조 기능이 있음(항목 50).

**확인된 한계 — 지금 없는 것** (실제 워크플로위와 비교):
- 접기/펼치기(collapse) 없음 — 항목이 많아지면 전체가 한 화면에 계속
  펼쳐져 있어야 함.
- 확대(zoom-in, 특정 항목만 화면 최상단으로 올려 그 하위만 집중해서
  편집) 없음.
- 항목 이동(드래그 또는 단축키로 위/아래 순서 바꾸기, 다른 상위 항목
  밑으로 옮기기) 없음 — 순서를 바꾸려면 직접 잘라 붙여야 함.
- 검색/필터 없음.
- 완료 표시(체크박스·취소선) 없음.
- 항목별 메모(note, 제목 줄과 별도로 붙는 보조 설명 한두 줄) 없음.
- 태그(#태그) 입력·자동완성 없음 — 붙여넣기 정리에서 "인식"만 하고,
  이 아웃라이너 자체에서 입력하는 기능은 없음.
- 미러(같은 항목을 여러 위치에 복제해 두면 한쪽 수정 시 같이 바뀜)
  없음 — 우선순위 낮은 고급 기능으로 분류.
- 저장된 아웃라인을 나중에 다시 열어 이어 쓰는 기능 없음 — 창을 열
  때마다 빈 문서로 시작하고, "추가" 버튼을 누르면 파일로 저장 후 바로
  문서 목록에 등록되는 1회성 작성 도구에 가깝다. 이어 쓰기를 하려면
  저장된 .md 파일을 파일 탐색기에서 다시 열어 붙여넣는 수밖에 없음.

**다음 단계 전 확인 필요**: 위 목록 중 실제로 강화하고 싶은 항목(전부는
아닐 가능성이 큼)을 사용자에게 우선순위로 받아야 함. 특히 "이어 쓰기
(저장된 아웃라인 다시 열기)"는 이 도구의 "1회성 작성 후 바로 내보내기"
설계 전제 자체를 바꾸는 큰 변경이라 별도로 다시 논의가 필요해 보임.

### [6순위, 재검토 필요] 윈도우 스티커 메모 연동

**A 방향 확정에 따른 재검토**: 8단계 시간 비중 어디에도 명확히 걸리지
않는 아이디어다. A(서식 자동화 심화) 기준으로 우선순위를 매기면 가장
낮다 — 착수 전에 "왜 필요한지"(어느 업무 단계를 줄여주는지)부터 다시
확인 필요.

**아이디어만 있고 구체 요구사항은 아직 없음** — 방향(스티커 메모 →
이 앱, 이 앱 → 스티커 메모, 또는 양방향)조차 확인되지 않았다. 다만
기술적으로 조사해 둔 사실:
- Windows 10/11의 스티커 메모(UWP 앱)는 메모를 텍스트 파일이 아니라
  SQLite DB(`%LocalAppData%\Packages\Microsoft.MicrosoftStickyNotes_8wekyb3d8bbwe\LocalState\plum.sqlite`)
  에 저장한다. `Note` 테이블에 메모 내용이 들어 있고(정확한 스키마·
  서식 인코딩은 구현 시 실 파일로 직접 확인 필요), 트랜잭션 로그가
  같은 폴더의 `plum.sqlite-shm`/`plum.sqlite-wal`에 남는다.
- 공식 API는 없고, 포렌식 도구들(예: StickyParser)이 이 sqlite 파일을
  직접 읽는 방식으로 동작한다 — 이 앱도 같은 방식(`sqlite3` 표준
  라이브러리로 직접 읽기)이 유일한 현실적 경로로 보임.

**다음 단계 전 확인 필요**: 무엇을 하고 싶은 기능인지부터 정해야 함 —
예를 들어 (a) 스티커 메모에 적어 둔 개조식 메모를 이 앱의 "정리할
문서" 목록에 바로 추가하는 기능인지, (b) 이 앱이 처리한 결과 요약을
스티커 메모로 남기는 기능인지, (c) 완전히 다른 용도인지에 따라 필요한
작업이 크게 달라짐.
